worker_processes 1;
events { worker_connections 1024; }

http {
    server {
        listen <%= ENV["PORT"] %>;
        server_name _;
        root /app/dist; # This is where Vite outputs built project by default
        index index.html;
        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
